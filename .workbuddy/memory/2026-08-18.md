# 2026-08-18

## Papertrack 工作计划（用户决策）
- 用户确认：课题组管理员设为**自己（用户本人）当 admin**，导师默认普通成员（可后续升 admin）。
- 可见性：**仅本课题组成员登录后才可见进度**，非成员看不到（不做公开只读链接）。
- 要求把旧的微信小程序 `-3 天` 逾期 bug 连同网页版一起修。

## 小程序（D:\Coding\Papertrack）修复 -3 天 bug
- `services/paperService.js` `decoratePaper`：新增 `deadlineText`（已逾期 N 天 / 距截止 N 天）、`deadlineOverdue`；`deadlineUrgent` 改为仅未来 ≤7 天。
- `components/paper-card/paper-card.wxml`：改用 `paper.deadlineText`，逾期也标红。
- `pages/paper-detail/paper-detail.js`：详情页 `deadlineHint` 改用 `paper.deadlineText`，移除无用 `revisionDays` 变量。

## 网页版（D:\Coding\papertrack-web）升级为多团队 + 邀请码自助注册
- `schema.sql`：新增 teams / team_members / invite_codes；papers 加 team_id、revision_deadline；**移除全局 admin 种子**。
- `functions/_lib/auth.js`：加 `memberRole` / `getUserTeams` / `startSession`。
- 新增 `register.js`（创建课题组 / 凭邀请码加入）、`teams.js`、`teams/[id]/invites.js`、`teams/[id]/members.js`。
- `login.js` / `me.js` 返回团队列表；`papers.js` 改为按 team_id 隔离、成员可见、归属校验、含返修截止字段。
- 移除旧的全局 `users.js` / `users/[id].js`（被团队成员管理替代）。
- 前端：`index.html` / `app.js` / `styles.css` 重写——注册弹窗（双 Tab）、课题组切换、课题组管理（成员+邀请码）、返修截止倒计时（正确显示已逾期）。
- 验证：10 个后端函数 + app.js 语法通过；前端 61 引用 ID 全部匹配 HTML；静态服务 200。npm 沙箱无网，未跑 wrangler 端到端。

## 部署准备（用户下一步动作）
- 修正 `wrangler.toml` 旧注释（曾写 admin/papertrack 默认账号，与多团队「注册自建 admin」矛盾）。
- 新增 `部署傻瓜清单.md`：Cloudflare 免费账号 → 装 wrangler → `d1 create` 拿 id → 填 wrangler.toml → `d1 execute schema.sql` → `pages deploy` → 创建课题组拿邀请码发给 4 人。

## 本地预览服务（临时，仅供用户看界面）
- 新增 `preview-server.cjs`：纯 Node 零依赖、零联网，内存模拟后端接口 + 预置数据（课题组 t1、成员 admin/师姐/师妹、5 篇论文，其中一篇返修截止 2026-08-10 已过期为演示「已逾期」效果）。
- 后台运行于 `http://localhost:4173/`，登录 admin/admin123（演示用）。仅本机可见，不能分享给导师；真分享需走 Cloudflare 部署。
- 注意：沙箱 npm 无法联网安装 wrangler，预览改用零依赖方案绕开；用户本机网络好可自行装 wrangler 部署。
- 2026-08-18 09:09：用户反馈内置浏览器打开 localhost:4173 显示 404。排查发现服务实际运行正常（curl GET / 返回 200），可能首次打开时请求路径异常或服务瞬态。已修复：preview-server.cjs 加请求日志、路径归一化、404 兜底回退 index.html；重启后重新 present，日志显示 GET / 与 HEAD / 均命中。

## 2026-08-18 09:20 视觉优化（用户选 UI Designer expert）
- 用户反馈页面丑、表格换行，要求加宽并尽量保持一行。
- 重写 `public/styles.css`：容器从 1100px 扩到 1600px；表格改用 `table-layout: fixed` 并为 10 列分配固定百分比宽度（论文题目 22%、期刊 18% 等，合计 100%）；题目/期刊/数字/操作等关键单元格 `white-space: nowrap` + 省略号，避免换行。
- 调整整体配色为更克制的靛蓝+石板灰（降低饱和度）；统计卡改顶部细线标识；按钮、弹窗、徽章、提示文字统一间距与圆角；优化移动端卡片式响应。
- 重新启动预览服务并 present `http://localhost:4173/`。

## 2026-08-18 09:30 两个新功能（用户要求两者都要）
- 需求：① 一键把成员升为管理员（之前只能靠发管理员邀请码）；② 每个成员小框加「+ 添加论文」快捷入口。
- 后端：`teams/[id]/members.js` 新增 `onRequestPut` 改角色（仅 admin、不能改自己、role 仅 admin/member）；`papers.js` POST 允许该组 admin 通过 `owner_id`+`owner_name`（校验确为本组成员）把论文归属指定成员，否则归属自己。
- 预览服务器 `preview-server.cjs` 同步：members PUT、papers POST owner 覆盖。
- 前端 `app.js`：`openModal(record, ownerId, ownerName)` 支持指定归属；新增 `openAddFor`、`setMemberRole`；成员列表渲染「设为管理员/降为成员」按钮；`renderBoards` 每个 owner-card 头部加「+ 添加论文」（本人或 admin 可见）；`submitPaper` 在 POST 时带上 owner 覆盖；点击委托处理 `data-addid` / `data-promote` / `data-demote`。
- `styles.css` 加 `.add-for-btn`、`.user-row-actions`。
- 验证实测：admin 登录→升师姐为 admin 成功→代填一篇师姐论文归属正确→师妹越权升管理员被拒（「仅管理员可修改成员角色」）。语法 + ID 匹配全通过。重启预览 present `http://localhost:4173/`。

## 2026-08-18 09:57 筛选区升级为多维度组合筛选
- 用户需求：原来的「进度」「成员」两个单选下拉太单薄，希望打开下拉后能多选/多组合，且不止这两个维度。
- 前端 `index.html`：工具栏替换两个 `<select>` 为 5 个自定义下拉组（进度/成员/分区/截止时间/排序），每个含 trigger 按钮 + hidden 下拉面板。
- 前端 `app.js`：`state.filters` 改为 `{status:Set, owner:Set, quartile:Set, deadline, sort}`；`loadPapers` 改为只拉本组全部论文（`applyFilters` 前端本地过滤+排序）；新增 `applyFilters`（文本搜索 + 四维过滤 + 排序）、`renderFilterBar`、`buildChecklist`（多选+全选/清空）、`buildRadio`（单选：截止时间、排序）、`toggleFilterDropdown`/`closeFilterDropdowns`；`renderBoards` 改用 `state.filtered`（含空状态下区分「无数据」与「筛选无匹配」）。搜索输入改为触发 applyFilters。
- 筛选维度：进度（多选：草稿/已投稿/审稿中/返修/接收/拒稿/已发表）、成员（多选）、分区（多选 Q1-Q4/待定）、截止时间（单选：全部/已逾期/今天到期/7天内/30天内）、排序（最近更新/投稿时间/返修截止/影响因子/负责人）。
- 交互：勾选一个条件后下拉保持打开，可继续勾选其他条件；触发按钮实时显示已选摘要（如「审稿中、返修」），点击外部关闭。
- `styles.css`：新增 `.filter-group/.filter-trigger/.filter-dropdown/.filter-opt/.filter-foot/.filter-mini/.filter-radio` 等样式，复用既有靛蓝主题；下拉面板用 `:has(.filter-dropdown:not(.hidden))` 高亮当前打开的触发器。
- 后端无需改动（前端不再传 status/owner 查询参数，只传 team_id）；`preview-server.cjs` 兼容（GET /papers?team_id= 返全部）。种子数据已含 5 状态/3 成员/Q1-Q2/1 条已逾期，足以演示。
- 验证：app.js + preview-server.cjs `node --check` 通过；无 filterStatus/filterOwner 旧引用残留；静态资源 200；预览服务运行于 `http://localhost:4173/`。
