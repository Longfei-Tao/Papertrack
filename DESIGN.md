# PaperTrack 设计系统

> 课题组科研论文投稿协作管理平台 · 微信小程序
> 设计关键词：学术 · 专业 · 冷静 · 简洁 · 可信赖

本文件是全局设计约定的唯一来源。所有页面与组件均基于以下令牌实现，避免各自为政导致风格不统一。

## 1. 色彩

采用低饱和「学术蓝」为主色，克制、不刺眼，辅以少量状态色。

| 令牌 | 值 | 用途 |
| --- | --- | --- |
| `--pt-primary` | `#3563A6` | 主色：按钮、选中态、品牌 |
| `--pt-primary-deep` | `#27497F` | 主色深：文字链接、按下态 |
| `--pt-primary-soft` | `#EDF2FA` | 主色浅：标签底、浅背景 |
| `--pt-bg` | `#F6F7F9` | 页面背景（近白极浅灰） |
| `--pt-surface` | `#FFFFFF` | 卡片 / 面板 |
| `--pt-text-1` | `#1B2233` | 主文本 |
| `--pt-text-2` | `#667080` | 次文本 |
| `--pt-text-3` | `#98A1B0` | 弱文本 / 占位 |
| `--pt-border` | `#EAEDF1` | 边框 |
| `--pt-divider` | `#F0F2F5` | 分割线 |
| `--pt-danger` | `#B5584E` | 删除 / 危险（暗红） |
| `--pt-warning` | `#C08A3E` | 提醒（赭黄，用于返修截止） |
| `--pt-success` | `#3F8E5F` | 成功（绿） |

### 状态色（投稿中文分类）

克制、低饱和。定义见 `utils/constants.js` 的 `STATUS_CATEGORIES`。

| 分类 | 颜色 |
| --- | --- |
| 准备中 | `#6B7686` |
| 已投稿 | `#4A7BB8` |
| 编辑处理中 | `#4F6D95` |
| 审稿中 | `#5B6AB8` |
| 返修中 | `#C08A3E` |
| 已接收 | `#3F8E5F` |
| 已发表 | `#2E7D52` |
| 已拒稿 | `#B5584E` |
| 已撤稿 | `#8A8F98` |

## 2. 字体

微信小程序使用系统字体（iOS 苹方 / Android 思源黑体 / 微软雅黑），通过字号与字重建立层级。

| 令牌 | 值 | 用途 |
| --- | --- | --- |
| `--pt-fs-xl` | `40rpx` | 页面大标题 |
| `--pt-fs-lg` | `34rpx` | 论文标题 / 卡片标题 |
| `--pt-fs-md` | `30rpx` | 正文 |
| `--pt-fs-sm` | `26rpx` | 辅助（期刊、作者、日期） |
| `--pt-fs-xs` | `22rpx` | 标签、小字 |

层级原则：**论文标题 > 期刊名 > 状态标签 > 作者/日期**。老师能快速扫描大量论文。

## 3. 间距

8 的倍数：`8 / 12 / 16 / 20 / 24 / 28 / 32 / 40 rpx`。页面统一留白 `24rpx`。

## 4. 圆角与阴影

- 卡片：`16rpx`
- 按钮 / 输入框：`12rpx`
- 标签：`8rpx`（胶囊用 `999rpx`）
- 阴影：`0 2rpx 12rpx rgba(27,34,51,0.05)`（极轻，配合细边框使用）

## 5. 组件规范

| 组件 | 说明 |
| --- | --- |
| `status-tag` | 状态标签：色点 + 中文分类 |
| `paper-card` | 论文卡片：左侧状态色条 + 标题/期刊/分区/状态/作者/负责人/更新时间/返修截止 |
| `stat-card` | 概览数字卡：数字 + 中文标签，可点选筛选 |
| `empty-state` | 空状态：简约插画 + 主文案 + 辅助说明 + 操作按钮 |
| `timeline` | 投稿时间线：纵向节点 + 连接线，当前状态高亮 |
| `member-avatar` | 成员头像：首字圆形色块 |
| `paper-form` | 论文表单（新增/编辑复用，分区布局） |

## 6. 设计语言参考

信息层级、留白、卡片、字体关系参考 Linear / Notion / Apple Settings / Raycast 与科研 SaaS，**只借鉴结构，不复制视觉**。

## 7. 语言原则

- 界面、导航、按钮、提示、表单、状态解释、日期、统计、空状态**全部简体中文**。
- 用户录入的科研信息（期刊名、论文标题、投稿系统英文状态）**保留原文**，不翻译。

## 8. 图标与插画

- 第一版 TabBar 使用纯文字（原生图标需 PNG，正式版补齐）。
- 插画用 CSS 简约绘制（空状态、欢迎页 Logo），保持低饱和学术蓝配色，避免装饰性堆砌。
